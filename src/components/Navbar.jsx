import React from 'react'
import { NavLink } from 'react-router-dom';
const Navbar = () => {
  return (
    <nav>
        <ul>
            {/* <li><NavLink to="/" exact activeClassName="active">Home</NavLink></li>
            <li><NavLink to="/about" exact activeClassName="active">About</NavLink></li>
            <li><NavLink to="/skills" exact activeClassName="active">Skills</NavLink></li>
            <li><NavLink to="/projects" exact activeClassName="active">Projects</NavLink></li>
            <li><NavLink to="/contact" exact activeClassName="active">Contacts</NavLink></li> */}
        </ul>
    </nav>
  )
}

export default Navbar